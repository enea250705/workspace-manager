{
  "version": 2,
  "buildCommand": "cd client && npm install --legacy-peer-deps --no-audit && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "echo Skipping root install",
  "framework": "vite",
  "github": {
    "silent": true
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
} 